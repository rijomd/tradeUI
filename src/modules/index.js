import auth from './auth';
import home from './home';
import dashBoard from './dashBoard';

const containers = {
    auth: auth,
    home: home,
    dashBoard: dashBoard
};

export default containers;
